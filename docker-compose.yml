version: '3.8'

services:
  app:
    build: .
    container_name: pdi_app
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
